package visualization.snake;

import javafx.scene.image.ImageView;

public class MainBody extends SnakeBodyPart {
    private ImageView mainBodyImage;

    public MainBody(int size, int startPositionX, int startPositionY) {
        super(size, startPositionX, startPositionY);
        this.mainBodyImage = mainBodyImage;
    }
}
